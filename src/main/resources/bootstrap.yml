spring:
  application:
    name: backend
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  profiles: 
    active:
    - dev
    
server:
  port: 8080

app:
  jwt:
    header: Authorization
    
logging:
  level:
    org.hibernate.sql: DEBUG
    