spring:
  application:
    name: backend
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  profiles: 
    active:
    - dev
    
server:
  port: 8181
    
logging:
  level: 
    org.jeavio.meetin.backend: DEBUG
